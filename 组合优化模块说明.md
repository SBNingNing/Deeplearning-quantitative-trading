# 投资组合优化与风险控制模块说明

本项目在截面多因子排序和缓冲选股规则的基础上，引入了带有换手率惩罚的全局最小方差（GMV）模型，并结合目标波动率约束机制，以精细化管理策略的总体风险暴露与调仓摩擦成本。相关数学模型与求解逻辑位于 `src/portfolio.py`，完整集成于 `src/backtest.py`。

## 1. 带有换手率惩罚与约束的全局最小方差模型 (Constrained GMV Optimization)

在确定 $N$ 只入选资产后，为了规避传统均值-方差模型（Mean-Variance）对预期收益率估算误差极度敏感的问题，本策略转而采用鲁棒性更强的**全局最小方差（Global Minimum Variance, GMV）模型**。

模型利用个股在过去 $k$ 个（默认 60 个）交易日的日频历史收益率序列，计算股票协方差矩阵 $\Sigma$。在此基础上，为了平滑交易调仓频率、规避高昂换手率导致的净值损耗，目标函数增加了一个 L1 范数的调仓惩罚偏差项，定义为组合预期方差与换手惩罚项之和极小化：

$$
\min_{w} \left( w^T \Sigma w + \lambda \sum_{i=1}^{N} |w_i - w_{i, prev}| \right)
$$

其中：
* $\lambda$ 为设定好的调仓惩罚系数参数，默认为 0.01。
* $w_{i, prev}$ 表示前一个交易日留存下来的起始资产持有权重。对新进入的资产该项通常为零。

进一步，为防备优化结果过度倚重单只个券且禁止做空，针对权重数组 $w_i$ 规划了严格下限（强制分散化投资）与上限阻断边界：

$$
\begin{cases} 
\sum_{i=1}^{N} w_i = 1 & (\text{全额投资预算约束}) \\ 
0.05 \le w_i \le 0.35, \quad \forall i & (\text{单一资产权重的严格下/上限}) 
\end{cases}
$$

**求解方案**：此最优化问题本质上涉及非线性（或有二次型加绝对值）的带约束凸优化过程，本系统使用基于 SciPy 封装的非线性规划求解器 **SLSQP (Sequential Least Squares Programming)** 加以数值求解。若因数据结构奇点造成数值解抛出异常，策略会自动退回等权重分配以确保持仓安全。

## 2. 目标波动率控制 (Target Volatility Control)

在应对牛熊急剧切换及极端系统性风险层面，组合获取优化后的权重比例向量 $w^*$ 后，将进行一次针对“宏观风险阀值”的健康度审验。首先量化组合的前瞻预期年化波动率：

$$
\sigma_p = \sqrt{w^{*T} \Sigma w^* \times 252}
$$

设定硬性目标年化波动率红线为 $\sigma_{target} = 0.12$（即12%）。
若测算得该投资组合的年化波动率警报被触发出界（即 $\sigma_p > 0.12$），策略机制将介入干预执行强制“降波”，将现有风险资产按照偏离系数进行无差别等比折减缩小：

$$
w_{final} = w^* \times \frac{0.12}{\sigma_p}
$$

通过此步骤后得到的调整资产总权和 $\sum w_{final} < 1$。此折算差额留存出来的虚转资金，即被视同安全转划至无风险资产（Cash 现金及等价物）。利用这部分留白有效对冲系统性向下倾泻造成的冲击幅度。

## 3. 回测对接说明

你可以通过将 `src/backtest.py` 的核心入口函数的配置更改为本模块挂载配置去进行测试验证。开启新模块需要的调整参数如下：

- `weight_method="gmv"` （将模式从默认的简单等权 `"equal"` 修改为 `"gmv"`，即可激活该算法组件层）
- 以下调参均拥有妥善初始默认设置：
  - `gmv_volatility_target`: `0.12` （降波线）
  - `gmv_penalty_lambda`: `0.01` （换手阻力系数）
  - `gmv_cov_window`: `60` （滑窗构建协方差矩阵的回看天数）
  - `gmv_min_weight`: `0.05` （防踩踏单一资产最小占用率 5%）
  - `gmv_max_weight`: `0.35` （防单票极端超配占率 35%）

**期望效果**：结合该权重优化管理后，在维持相当收益规模水平前提下理论可以获得一条更为平滑陡峭的资金曲线，回测面板中的最大回撤（Max Drawdown）应有所下降，夏普与卡玛比率（Sharpe / Calmar）取得进步。